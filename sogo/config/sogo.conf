{
	// Run options
	WOPort = "0.0.0.0:20000";
	WOLogFile = "-";
	WONoDetach = YES;
	// Database
	SOGoProfileURL = "mysql://sogo:sogo@mariadb:3306/sogo/sogo_user_profile";
	OCSFolderInfoURL = "mysql://sogo:sogo@mariadb:3306/sogo/sogo_folder_info";
	OCSSessionsFolderURL = "mysql://sogo:sogo@mariadb:3306/sogo/sogo_sessions_folder";
	MySQL4Encoding = "utf8mb4";
	// Cache
	SOGoMemcachedHost = "memcached";
	// UI
	SOGoFirstDayOfWeek = 0;
	SOGoLoginModule = "Calendar";
	WOWorkersCount = 5;
	// SSO
	SOGoTrustProxyAuthentication = YES;
	// Default rights when sharing a calendar
	SOGoCalendarDefaultRoles = ("PublicViewer", "ConfidentialDAndTViewer");
	// Allow users make their calendars and address book public
	SOGoEnablePublicAccess = YES;
	// Users
	SOGoUserSources = ({
		type = ldap;
		id = "flap.local";
		CNFieldName = cn;
		IDFieldName = sn;
		UIDFieldName = sn;
		baseDN = "ou=users,dc=flap,dc=local";
		bindDN = "cn=admin,dc=flap,dc=local";
		bindPassword = "admin"; // TODO
		canAuthenticate = YES;
		hostname = "ldap://ldap";
		// Prevent access to the Mail module
		// TODO - add mail support to FLAP and allow the Mail module
		// ou = none means that the ou of the user must be none, which will never be the case
		ModulesConstraints = {
			Mail = {
				ou = none;
			};
		};
	});
}
